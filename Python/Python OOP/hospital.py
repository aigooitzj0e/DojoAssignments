class hospital(object):
	def __init__(self,hospital_name, capactiy):
		self.patient_list = []
		self.hospital_name = hospital_name
		self.capacity = capacity
		self.bed_num = self.count
		self.count = 0

	def admit(self):
		self.patient_list.append(patient_name) #append patient name to patient list
		self.count += 1
		print bed_num
		
	def discharge(self):
		self.search = patient_list.index(patient_name) #searches to see what index the patient is at in list. returns index #
		self.patient_list.pop(self.search) #removes patient at designated index
